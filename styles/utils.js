import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  center_content: {
    justifyContent: 'center',
    alignItems: 'center',
  },

  full_size: {
    width: '100%',
    height: '100%',
  },
})
